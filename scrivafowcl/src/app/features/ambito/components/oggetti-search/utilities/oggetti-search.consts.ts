/*
 * ========================LICENSE_START=================================
 * 
 * Copyright (C) 2025 Regione Piemonte
 * 
 * SPDX-FileCopyrightText: (C) Copyright 2025 Regione Piemonte
 * SPDX-License-Identifier: EUPL-1.2
 * =========================LICENSE_END==================================
 */

/**
 * Classe che rappresenta le costanti dell'omonimo componente.
 */
export class OggettiSearchConst {
  /** string con la definizione del campo del form per la ricerca dati. */
  PROVINCIA: string = 'provincia';
  /** string con la definizione del campo del form per la ricerca dati. */
  ID_COMUNE: string = 'id_comune';
  /** string con la definizione del campo del form per la ricerca dati. */
  DEN_OGGETTO: string = 'den_oggetto';
  /** string con la definizione del campo del form per la ricerca dati. */
  COD_SCRIVA: string = 'cod_scriva';
  /** string con la definizione del campo del form per la ricerca dati. */
  ANNO_PRESENTAZIONE: string = 'anno_presentazione';
  /** string con la definizione del campo del form per la ricerca dati. */
  CF_SOGGETTO: string = 'cf_soggetto';

  /**
   * Costruttore.
   */
  constructor() {}
}
